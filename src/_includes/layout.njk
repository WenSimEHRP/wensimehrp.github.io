<!DOCTYPE html>
<html lang="en" class="scroll-smooth">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or metadata.title }}</title>
  <meta name="description" content="{{ metadata.description }}">
  <meta name="author" content="{{ metadata.author.name }}">
  <link rel="canonical" href="{{ metadata.url }}">
  <link href="/css/output.css" rel="stylesheet">
</head>

<body
  class="bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-gray-100 antialiased min-h-screen flex flex-col transition-colors">
  {% include "components/dark-mode-toggle.njk" %}
  {% include "components/header.njk" %}

  <!-- Main Content -->
  <main class="flex-1 pb-20">
    <div class="max-w-7xl mx-auto px-6 py-12 flex gap-8">
      <!-- Article Content -->
      <article class="flex-1 max-w-4xl pl-4 pr-8">
        <div class="prose prose-gray dark:prose-invert max-w-none">
          <h1>{{ title }}</h1>
          {% if description %}
          <i>{{ description | safe }}</i>
          {% endif %}
          {{ content | safe }}
        </div>

        {% include "components/date-info.njk" %}
      </article>

      {% include "components/toc.njk" %}
    </div>
  </main>

  {% include "components/floating-footer.njk" %}
  {% include "components/stable-footer.njk" %}

  {% include "components/toc-script.njk" %}
</body>

</html>
